<header
    class="sticky top-0 z-50 flex h-16 w-full items-center justify-between bg-white dark:bg-gray-900 px-4 shadow-sm sm:px-6"
>
    <div class="flex items-center space-x-4">
        <!-- Logo -->
        <div class="text-xl font-bold text-gray-900 dark:text-white">El servidor cat</div>
    </div>
    <!-- Barra de cerca -->
    <div class="flex-1 mx-4">
        <input
            type="text"
            placeholder="Cercar..."
            class="w-full px-4 py-2 border rounded-md bg-gray-100 dark:bg-gray-800 border-transparent focus:ring-2 focus:ring-indigo-500 focus:border-transparent dark:text-white"
        />
    </div>
    <!-- Perfil d'usuari -->
    <div class="flex items-center space-x-4">
        <img
            src="ruta/a/la/foto-de-perfil.jpg"
            alt="Perfil"
            class="w-10 h-10 rounded-full"
        />
    </div>
</header>