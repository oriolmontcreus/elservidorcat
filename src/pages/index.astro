---
import PublicLayout from "@layouts/PublicLayout.astro";
import "@styles/app.css";
import { ModeWatcher } from "mode-watcher";
import { Icon } from "astro-icon/components";
import { ViewTransitions } from "astro:transitions";
import MagicCard from "@components/MagicCard.svelte";
---

<script is:inline>
  const isBrowser = typeof localStorage !== "undefined";
  const getThemePreference = () => {
    if (isBrowser && localStorage.getItem("theme")) {
      return localStorage.getItem("theme");
    }
    return window.matchMedia("(prefers-color-scheme: dark)").matches
      ? "dark"
      : "light";
  };
  const isDark = getThemePreference() === "dark";
  document.documentElement.classList[isDark ? "add" : "remove"]("dark");

  if (isBrowser) {
    const observer = new MutationObserver(() => {
      const isDark = document.documentElement.classList.contains("dark");
      localStorage.setItem("theme", isDark ? "dark" : "light");
    });
    observer.observe(document.documentElement, {
      attributes: true,
      attributeFilter: ["class"],
    });
  }
</script>

<PublicLayout title="El Servidor">
  <ModeWatcher client:load />
  <main class="flex flex-col items-center justify-center p-6 md:p-12 bg-gray-100 dark:bg-gray-900">
    <h1 class="text-2xl sm:text-3xl  my-5 font-bold text-center text-gray-900 dark:text-gray-100">
      Benvingut <span class="text-blue-500">user</span> a El Servidor
    </h1>
    <div class="flex flex-wrap justify-center gap-4 w-full max-w-[1200px] overflow-x-auto min-h-[200px]">
      <div class="flex flex-1 flex-col gap-4 lg:flex-row" style="min-height: 200px;">
        <MagicCard client:visible
          class="cursor-pointer flex-1 flex flex-col items-center justify-center  whitespace-nowrap text-xl sm:text-2xl group hover:border-[#ff2e2e9e] transition-all duration-300 px-10"
          lightGradientColor="#ffe0e0"
          darkGradientColor="#360404"
          gradientSize={300}
          href="/gamedesign"
        >
          <div
            class="group-hover:text-[#ff3c2e] transition-all duration-500 flex gap-1.5 justify-center items-center font-semibold"
          >
            <Icon name="lucide:gamepad" class="w-8 h-8 sm:w-10 sm:h-10 lg:w-12 lg:h-12 text-gray-700 dark:text-gray-300 group-hover:text-[#ff3c2e] transition-all duration-500" />
            <span>M15 - Game Design</span>
          </div>
        </MagicCard>
      </div>

      <div class="flex flex-1 flex-col gap-4 lg:flex-row" style="min-height: 200px;">
        <MagicCard client:visible
          class="cursor-pointer flex-1 flex flex-col items-center justify-center  whitespace-nowrap text-xl sm:text-2xl group hover:border-[#e67e22] transition-all duration-300 px-10"
          lightGradientColor="#fff3e0"
          darkGradientColor="#362504"
          gradientSize={300}
          href="/vr"
        >
          <div
            class="group-hover:text-[#e67e22] transition-all duration-500 flex gap-1.5 justify-center items-center font-semibold"
          >
            <Icon name="lucide:box" class="w-8 h-8 sm:w-10 sm:h-10 lg:w-12 lg:h-12 text-gray-700 dark:text-gray-300 group-hover:text-[#e67e22] transition-all duration-500" />
            <span>M14 - VR</span>
          </div>
        </MagicCard>
      </div>

      <div class="flex flex-1 flex-col gap-4 lg:flex-row" style="min-height: 200px;">
        <MagicCard client:visible
          class="cursor-pointer flex-1 flex flex-col items-center justify-center  whitespace-nowrap text-xl sm:text-2xl group hover:border-[#1259f3] transition-all duration-300 px-10"
          lightGradientColor="#e0f1ff"
          darkGradientColor="#040636"
          gradientSize={300}
          href="/interfaces"
        >
          <div
            class="group-hover:text-[#1259f3] transition-all duration-500 flex gap-1.5 justify-center items-center font-semibold"
          >
            <Icon name="lucide:laptop" class="w-8 h-8 sm:w-10 sm:h-10 lg:w-12 lg:h-12 text-gray-700 dark:text-gray-300 group-hover:text-[#1259f3] transition-all duration-500" />
            <span>M7 - Interfícies</span>
          </div>
        </MagicCard>
      </div>

      <div class="flex flex-1 flex-col gap-4 lg:flex-row" style="min-height: 200px;">
        <MagicCard client:visible
          class="cursor-pointer flex-1 flex flex-col items-center justify-center  whitespace-nowrap text-xl sm:text-2xl  group hover:border-[#27ae60] transition-all duration-300 px-10"
          lightGradientColor="#e0ffee"
          darkGradientColor="#043615"
          gradientSize={300}
          href="/networks"
        >
          <div
            class="group-hover:text-[#27ae60] transition-all duration-500 flex gap-1.5 justify-center items-center font-semibold"
          >
            <Icon name="lucide:network" class="w-8 h-8 sm:w-10 sm:h-10 lg:w-12 lg:h-12 text-gray-700 dark:text-gray-300 group-hover:text-[#27ae60] transition-all duration-500" />
            <span>M7 - Xarxes</span>
          </div>
        </MagicCard>
      </div>

      <div class="flex flex-1 flex-col gap-4 lg:flex-row" style="min-height: 200px;">
        <MagicCard client:visible
          class="cursor-pointer flex-1 flex flex-col items-center justify-center  whitespace-nowrap text-xl sm:text-2xlgroup hover:border-[#9b59b6] transition-all duration-300 px-10"
          lightGradientColor="#f5e0ff"
          darkGradientColor="#1a0436"
          gradientSize={300}
          href="/project"
        >
          <div
            class="group-hover:text-[#9b59b6] transition-all duration-500 flex gap-1.5 justify-center items-center font-semibold"
          >
            <Icon name="lucide:briefcase" class="w-8 h-8 sm:w-10 sm:h-10 lg:w-12 lg:h-12 text-gray-700 dark:text-gray-300 group-hover:text-[#9b59b6] transition-all duration-500" />
            <span>M13 - Projecte</span>
          </div>
        </MagicCard>
      </div>
    </div>
  </main>
  <ViewTransitions />
</PublicLayout>